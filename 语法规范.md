# 自定义语言语法规范

## 一、基本语法

1. **语句结尾**  
   - 语句结尾**不使用分号 `;`**。  
   - 单条语句之间使用**换行**进行分隔。  
   - 同一行若存在多条语句且未使用换行分隔，将会报错。

2. **作用域与 `{}`**  
   - 使用花括号 `{}` 表示函数或代码块的作用域。  
   - 变量的生命周期被限制在其所在的 `{}` 作用域内。

3. **函数参数列表**  
   - 函数的参数列表使用 `()` 包裹。  
   - 参数之间使用逗号分隔。

4. **宏与条件编译**  
   - 支持类似 C++ 的宏定义和条件编译机制。  
   - 具体宏语法与条件编译指令由编译器规范进一步定义。

5. **内存操作**  
   - 语言的常规使用中**无需通过指针直接操作内存**。  
   - 内存管理由运行时或编译器负责。

---

## 二、变量声明

1. **基本类型**  
   - 提供以下基本变量类型：
     - `int`：整数类型  
     - `float`：浮点小数类型  
     - `char`：字符类型  
     - `bool`：布尔类型（真/假）

2. **类型推断**  
   - 声明基本变量时**不需要显式写出类型**，编译器将根据初始赋值自动推断类型。

3. **变量命名规则**  
   - 变量名可以由：
     - 英文字母  
     - 中文字符  
     - 数字  
     - 下划线 `_`  
     组成。  
   - 变量名**不能是纯数字**，也**不能是仅由下划线组成**。

4. **声明与赋值示例**  
   - 示例 1：

```text
a          // 第一次出现 a，为声明（尚未赋值）
a = 1      // 第二次出现，为赋值
```

   - 示例 2：

```text
a = 1      // 第一次出现 a，即声明并赋值
a = 2      // 第二次出现，为重新赋值
```

5. **常量 `const`**  
   - 在语句前加 `const` 表示声明常量。  
   - 常量在**声明时必须赋值**。  
   - 常量一旦赋值，之后**不可更改**，修改将导致编译错误。

   - 示例：

```text
const a = 1   // 第一次出现，为常量声明并赋值
a = 2         // 错误：常量不可更改
```

---

## 三、函数声明

1. **基本形式**  
   - 函数以关键字 `fn` 开头：

```text
fn 函数名(参数列表) {
    // 函数体
}
```

2. **返回值与 `return`**  
   - 使用 `return` 语句返回值。  
   - 无需在函数声明中显式写出返回值类型，返回类型由编译器根据 `return` 自动判断。  
   - 若同一函数中出现多种返回值类型，则编译报错。  
   - 返回值可以是：
     - 一个值（字面量或表达式结果）；
     - 一个变量的引用（见下文 `ref` 返回）。

3. **返回引用 `ref` 的限制**  
   - 可以通过 `return ref` 返回变量的引用：

```text
return ref x
```

   - 但**不能返回函数内局部变量的引用**，否则会产生悬垂引用错误。  
   - 只能返回其生命周期不短于当前函数的变量引用（例如外部作用域中的变量）。

4. **返回表达式的括号**  
   - 若返回值是一个表达式，需要使用括号包裹：

```text
return (a + b)
```

5. **函数命名规则**  
   - 函数名与变量名的命名规则相同：可使用英文、中文、数字、下划线，且不能是纯数字或纯下划线。

6. **参数列表与类型**  
   - 参数列表必须**显式指定参数类型**。  
   - 参数传递分为：
     - 值传递：默认方式；  
     - 引用传递：在参数前加 `ref` 关键字。

7. **函数示例**  

```text
fn myFunction(int a, float b, ref char c, bool d) {
    return 0
}
```

---

## 四、判断语句（if）

1. **基本用法**  
   - 使用 `if` 作为判断语句关键字。  
   - `if` 后**不需要**使用 `()` 包裹条件，直接编写表达式：

```text
if 条件 {
    // 条件为真时执行的语句
}
```

2. **条件类型与真值判断**  
   - 语言不是严格强类型的布尔判断，只要是基本类型即可出现在条件中。  
   - 除 `bool` 类型自身外，其它基本类型的真/假规则为：
     - `int`：值为 `0` 时为假，非 0 为真；  
     - `float`：值为 `0.0` 时为假，非 0.0 为真；  
     - `char`：值为 `'\0'` 时为假，其它字符为真。

3. **执行块**  
   - 判断为真后执行的语句使用 `{}` 包裹成代码块：

```text
if a {
    // 当 a 不为 0 时执行
}
```

---

## 五、循环语句

1. **循环种类**  
   - 语言中提供以下循环语句：
     - `while` 循环  
     - `for` 循环

2. **while 循环**  
   - `while` 后接条件表达式，与 `if` 的条件判断规则相同：

```text
while 条件 {
    // 循环体
}
```

   - 示例：

```text
while true {
    // 无限循环
}
```

3. **for 循环（迭代器语法）**  
   - `for` 后接迭代器，语法类似 Python 的区间迭代：

```text
for i in (起始值, 结束值, 步长) {
    // 使用 i 的循环体
}
```

   - 示例：

```text
for i in (1, 10, 1) {
    // i 从 1 开始，i == 10 时结束循环，每次 +1
}
```

   - 语义：
     - `i` 起始为 `起始值`；  
     - 当 `i == 结束值` 时结束循环；  
     - 每次循环后 `i` 增加 `步长`。

4. **可省略的参数**  
   - 可以省略**起始值**和**步长**，规则如下：
     - 省略**起始值**时，`i` 默认从 `0` 开始；  
     - 省略**步长**时，步长默认 `1`；  
     - **省略起始值时不能再省略步长**，即：
       - 合法：`for i in (0, 10, 1)`  
       - 合法：`for i in (1, 10)`（起始为 1，步长默认 1）  
       - 合法：`for i in (0, 10, 2)`  
       - 不合法示例：`for i in (, 10,)`（起始与步长都省略）。

5. **循环体**  
   - 循环体语句必须写在 `{}` 中：

```text
for i in (0, 10, 1) {
    // 每轮循环执行的语句
}
```

---

以上为该语言当前版本的基本语法规范，可在后续版本中扩展更多语法特性（如数组、结构体、模块系统等）。
