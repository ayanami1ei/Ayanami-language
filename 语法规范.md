一、基本语法
   1.所有语句都应当是以下几种：
      1）赋值语句
      2）函数声明
      3）函数返回
      4）控制流语句
      5）函数调用
   不能是单独的表达式。

   2.赋值语句
      1）形式为 变量 = 表达式
      2）变量的类型由编译器自动推导，无需显示写出
      3）每个变量实质上是指向对象的指针，允许同一变量指向不同类型的对象
      5）例：
         a=1      //int型
         a='c'    //char型

   3.函数声明
      1）形式为 fn 函数名(参数列表){}
      2）参数列表需要显式指出参数的类型
      3）可选ref修饰符来修饰参数，表示函数内对该对象的操作会影响到函数外
      4）返回值类型不固定，详见函数返回
      5）例：
         fn func(ref int a, char c){
            if a==1{
               return c    //返回char型
            } else {
               return a    //返回int型
            }
         }

   4.函数返回
      1）形式为 return 表达式
      2）函数的返回值类型将在这里确定，返回值是对象

   5.控制流语句
      1）if语句
         i）形式为 if cond1 {} elif cond2 {} elif ... else {}
         ii）条件必须是表达式
      2）for循环
         i）形式为 for itor in (start, end, step){}
         ii）itor是一个临时变量，仅在for的作用域内有效，初值为start
         iii）start，end和step是表达式
         ix）当itor大于等于end时，退出循环
         x）step是每次循环后itor的变化量，可以省略，默认为1
      3）while循环
         i）形式为 while cond {}
         ii）条件必须是表达式

   6.函数调用
      1）形式为 func(参数列表)
      2）可以视作表达式，也可以视作语句

二、面向对象（待开发）
   1.

内存管理系统：
   1.每个变量实质上是指向对象的指针
   2.记录变量指向的对象类型，当该变量不再使用指向的对象时，允许指向其它类型的变量
   3.通过分析对象的生存周期，在编译期尽量管理对象的创建和销毁
   4.编译期无法分析对象生成周期的，使用引用计数（Rc）来管理
   5.对于循环引用的数据结构，Rc不能处理的，使用垃圾回收（GC）来处理