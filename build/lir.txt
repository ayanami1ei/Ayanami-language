; ModuleID = 'ayanami_modlue'
source_filename = "ayanami_modlue"

@.str.0 = constant [3 x i8] c"str"
@.str.1 = constant [2 x i8] c": "
@.str.2 = constant [1 x i8] c" "

declare void @err(ptr)

declare i32 @get_int_value(ptr)

declare i1 @is_truth(ptr)

declare void @write(ptr)

declare i1 @is_type(i64, ptr)

declare void @del_obj(ptr)

declare void @dec_ref(ptr)

declare void @inc_ref(ptr)

declare ptr @alloc_int(i64)

declare ptr @alloc_float(double)

declare ptr @alloc_bool(i1)

declare ptr @alloc_char(i32)

declare ptr @alloc_array(i64)

declare ptr @alloc_string(ptr, i32)

declare ptr @add(ptr, ptr)

declare ptr @sub(ptr, ptr)

declare ptr @mul(ptr, ptr)

declare ptr @div_op(ptr, ptr)

declare ptr @equal(ptr, ptr)

declare ptr @greater(ptr, ptr)

declare ptr @less(ptr, ptr)

declare ptr @greater_equal(ptr, ptr)

declare ptr @less_equal(ptr, ptr)

declare ptr @and_op(ptr, ptr)

declare ptr @or_op(ptr, ptr)

declare ptr @not(ptr)

define i32 @main() {
entry:
  %var4 = alloca ptr, align 8
  %var3 = alloca ptr, align 8
  br label %block_0

block_0:                                          ; preds = %entry
  br label %block_1

block_1:                                          ; preds = %block_0
  %slotslot_6 = alloca ptr, align 8
  %slotslot_5 = alloca ptr, align 8
  %slotslot_4 = alloca ptr, align 8
  %slotslot_3 = alloca ptr, align 8
  %slotslot_2 = alloca ptr, align 8
  %"new int" = call ptr @alloc_int(i64 2)
  store ptr %"new int", ptr %slotslot_2, align 8
  %"new int1" = call ptr @alloc_float(double 2.100000e+00)
  store ptr %"new int1", ptr %slotslot_3, align 8
  %"new int2" = call ptr @alloc_char(i32 99)
  store ptr %"new int2", ptr %slotslot_4, align 8
  %"new string" = call ptr @alloc_string(ptr @.str.0, i32 3)
  store ptr %"new string", ptr %slotslot_5, align 8
  %alloc_array = call ptr @alloc_array(i64 4)
  store ptr %alloc_array, ptr %slotslot_6, align 8
  %arr.len = getelementptr inbounds { i32, i32, i32, ptr }, ptr %alloc_array, i32 0, i32 2
  store i32 4, ptr %arr.len, align 4
  %arr.data.ptr = getelementptr inbounds { i32, i32, i32, ptr }, ptr %alloc_array, i32 0, i32 3
  %arr.data = load ptr, ptr %arr.data.ptr, align 8
  %arr.elem.load = load ptr, ptr %slotslot_2, align 8
  %arr.elem.ptr = getelementptr inbounds ptr, ptr %arr.data, i64 0
  store ptr %arr.elem.load, ptr %arr.elem.ptr, align 8
  %arr.elem.load3 = load ptr, ptr %slotslot_3, align 8
  %arr.elem.ptr4 = getelementptr inbounds ptr, ptr %arr.data, i64 1
  store ptr %arr.elem.load3, ptr %arr.elem.ptr4, align 8
  %arr.elem.load5 = load ptr, ptr %slotslot_4, align 8
  %arr.elem.ptr6 = getelementptr inbounds ptr, ptr %arr.data, i64 2
  store ptr %arr.elem.load5, ptr %arr.elem.ptr6, align 8
  %arr.elem.load7 = load ptr, ptr %slotslot_5, align 8
  %arr.elem.ptr8 = getelementptr inbounds ptr, ptr %arr.data, i64 3
  store ptr %arr.elem.load7, ptr %arr.elem.ptr8, align 8
  %left_obj = load ptr, ptr %slotslot_6, align 8
  call void @inc_ref(ptr %left_obj)
  %bind.load = load ptr, ptr %slotslot_6, align 8
  store ptr %bind.load, ptr %var3, align 8
  br label %block_2

block_2:                                          ; preds = %block_1
  %slotslot_9 = alloca ptr, align 8
  %slotslot_8 = alloca ptr, align 8
  %slotslot_7 = alloca ptr, align 8
  %"new int9" = call ptr @alloc_int(i64 0)
  store ptr %"new int9", ptr %slotslot_7, align 8
  %left_obj10 = load ptr, ptr %slotslot_7, align 8
  call void @inc_ref(ptr %left_obj10)
  %bind.load11 = load ptr, ptr %slotslot_7, align 8
  store ptr %bind.load11, ptr %var4, align 8
  %"new int12" = call ptr @alloc_int(i64 4)
  store ptr %"new int12", ptr %slotslot_8, align 8
  %"new int13" = call ptr @alloc_int(i64 1)
  store ptr %"new int13", ptr %slotslot_9, align 8
  br label %block_3

block_3:                                          ; preds = %block_4, %block_2
  %slotslot_10 = alloca ptr, align 8
  %left_obj14 = load ptr, ptr %slotslot_7, align 8
  %right_obj = load ptr, ptr %slotslot_8, align 8
  %less = call ptr @less(ptr %left_obj14, ptr %right_obj)
  store ptr %less, ptr %slotslot_10, align 8
  %cond.load = load ptr, ptr %slotslot_10, align 8
  %"call is_truth" = call i1 @is_truth(ptr %cond.load)
  br i1 %"call is_truth", label %block_4, label %block_merge_5

block_4:                                          ; preds = %block_3
  %slotslot_18 = alloca ptr, align 8
  %slotslot_17 = alloca ptr, align 8
  %slotslot_16 = alloca ptr, align 8
  %slotslot_14 = alloca ptr, align 8
  %slotslot_15 = alloca ptr, align 8
  %slotslot_13 = alloca ptr, align 8
  %slotslot_12 = alloca ptr, align 8
  %slotslot_11 = alloca ptr, align 8
  %param.load = load ptr, ptr %var4, align 8
  store ptr %param.load, ptr %slotslot_11, align 8
  %"new string15" = call ptr @alloc_string(ptr @.str.1, i32 2)
  store ptr %"new string15", ptr %slotslot_12, align 8
  %left_obj16 = load ptr, ptr %slotslot_11, align 8
  %right_obj17 = load ptr, ptr %slotslot_12, align 8
  %add = call ptr @add(ptr %left_obj16, ptr %right_obj17)
  store ptr %add, ptr %slotslot_13, align 8
  %call.arg = load ptr, ptr %slotslot_13, align 8
  call void @write(ptr %call.arg)
  %param.load18 = load ptr, ptr %var4, align 8
  store ptr %param.load18, ptr %slotslot_15, align 8
  %param.load19 = load ptr, ptr %var3, align 8
  store ptr %param.load19, ptr %slotslot_14, align 8
  %arr.load = load ptr, ptr %slotslot_14, align 8
  %arr.data.ptr20 = getelementptr inbounds { i32, i32, i32, ptr }, ptr %arr.load, i32 0, i32 3
  %arr.data21 = load ptr, ptr %arr.data.ptr20, align 8
  %idx.obj = load ptr, ptr %slotslot_15, align 8
  %idx.value = call i32 @get_int_value(ptr %idx.obj)
  %idx.i64 = sext i32 %idx.value to i64
  %elem.ptr = getelementptr inbounds ptr, ptr %arr.data21, i64 %idx.i64
  %elem.load = load ptr, ptr %elem.ptr, align 8
  store ptr %elem.load, ptr %slotslot_16, align 8
  %"new string22" = call ptr @alloc_string(ptr @.str.2, i32 1)
  store ptr %"new string22", ptr %slotslot_17, align 8
  %left_obj23 = load ptr, ptr %slotslot_16, align 8
  %right_obj24 = load ptr, ptr %slotslot_17, align 8
  %add25 = call ptr @add(ptr %left_obj23, ptr %right_obj24)
  store ptr %add25, ptr %slotslot_18, align 8
  %call.arg26 = load ptr, ptr %slotslot_18, align 8
  call void @write(ptr %call.arg26)
  %left_obj27 = load ptr, ptr %slotslot_7, align 8
  %right_obj28 = load ptr, ptr %slotslot_9, align 8
  %add29 = call ptr @add(ptr %left_obj27, ptr %right_obj28)
  store ptr %add29, ptr %slotslot_7, align 8
  %bind.load30 = load ptr, ptr %slotslot_7, align 8
  store ptr %bind.load30, ptr %var4, align 8
  %load = load ptr, ptr %slotslot_12, align 8
  call void @del_obj(ptr %load)
  store ptr null, ptr %slotslot_12, align 8
  %load31 = load ptr, ptr %slotslot_13, align 8
  call void @del_obj(ptr %load31)
  store ptr null, ptr %slotslot_13, align 8
  %load32 = load ptr, ptr %slotslot_17, align 8
  call void @del_obj(ptr %load32)
  store ptr null, ptr %slotslot_17, align 8
  %load33 = load ptr, ptr %slotslot_18, align 8
  call void @del_obj(ptr %load33)
  store ptr null, ptr %slotslot_18, align 8
  br label %block_3

block_merge_5:                                    ; preds = %block_3
  %slotslot_19 = alloca ptr, align 8
  %"new int34" = call ptr @alloc_int(i64 0)
  store ptr %"new int34", ptr %slotslot_19, align 8
  %load35 = load ptr, ptr %slotslot_7, align 8
  call void @del_obj(ptr %load35)
  store ptr null, ptr %slotslot_7, align 8
  %load36 = load ptr, ptr %slotslot_9, align 8
  call void @del_obj(ptr %load36)
  store ptr null, ptr %slotslot_9, align 8
  %load37 = load ptr, ptr %slotslot_8, align 8
  call void @del_obj(ptr %load37)
  store ptr null, ptr %slotslot_8, align 8
  %ret.load = load ptr, ptr %slotslot_19, align 8
  call void @inc_ref(ptr %ret.load)
  %"get main ret" = call i32 @get_int_value(ptr %ret.load)
  call void @del_obj(ptr %ret.load)
  ret i32 %"get main ret"
}
