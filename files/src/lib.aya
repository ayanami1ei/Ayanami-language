pub struct VecInt {
    unique [int] inner
    int len
}

impl VecInt {
    pub fn push(unique self, int n) {
        tmp = unique [int; self.len + 1];
        for i in (0, self.len) {
            tmp[i] = self.inner[i];
        }
        self.inner = tmp;
        self.len = self.len + 1;
    }
}
